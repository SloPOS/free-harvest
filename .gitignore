# ESP-IDF build output
/build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Staged release BINARIES belong on the GitHub release, not in git - but the
# notes and publish script are source and should be versioned.
/dist/**
!/dist/**/
!/dist/**/*.md
!/dist/**/*.sh

# host test binaries
*.exe
*.o
*.obj

# editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
